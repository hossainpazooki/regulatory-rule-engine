//! ke: command-line entrypoint for ke-workbench.
//!
//! Subcommands `compile`, `verify`, `attest`, `publish`, `query`, `serve`
//! land in Gates 4 and 5. See spec § 16, 19.

fn main() {
    eprintln!("ke-cli scaffold (Gate 0). Subcommands not yet implemented.");
}
